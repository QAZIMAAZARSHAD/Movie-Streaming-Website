{
  "name": "mochaselenium",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "mocha --timeout 6000 --reporter mochawesome --require mochawesome/register --reporter-options reportDir=./report,reportFileName=SeleniumReport"
  },
    "directories": {
    "test": "./test"
  },
    "engines": {
    "node": ">=10.0.0"
  },
  "license": "ISC",
  "dependencies": {
    "chromedriver": "latest",
    "mocha": "latest",
    "mochawesome": "^7.1.3",
    "selenium-webdriver": "^4.4.0"
  }
}
